{
  "name": "nes-tetris-ai",
  "version": "0.0.1",
  "author": "Michał Budzyński <michal@virtualdesign.pl>",
  "repository": "git@github.com:michalbe/nes-tetris-ai.git",
  "scripts": {
    "compile:watch": "tsc --watch -p .",
    "lint": "prettier --check \"src/**/*.ts\"",
    "pretty": "prettier --write \"src/**/*.ts\"",
    "serve": "live-server --port=1234 --no-browser --quiet public",
    "start": "npm-run-all --parallel compile:watch serve"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "prettier": "^1.19.1",
    "typescript": "^3.7.2"
  }
}
